<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ns.sql.BookMapper">

	<sql id="select-book">
		SELECT isbn, title, catalogue, nation,
		publish_date, publisher, author, price, currency, description
		FROM book
	</sql>
	
	<insert id="insertBook" parameterType="book" >	
		INSERT INTO 
		book (isbn, title, catalogue, nation,
		publish_date, publisher, author, price, currency, description)
		VALUES(
			#{isbn},
			#{title},
			#{catalogue},
			#{nation},
			#{publishDate},
			#{publisher},			
			#{author},	
			#{price},
			#{currency},			
			#{description}			
		)
	</insert>
	
	<select id="getBooks" resultType= "book">	
		<include refid="select-book" />
	</select>
	
	<select id="searchByTitle" parameterType="string" resultType= "book">	
		<include refid="select-book" />
		WHERE title LIKE CONCAT('%', #{VALUE}, '%')
	</select>
	
	<select id="searchByPublisher" parameterType="string" resultType= "book">	
		<include refid="select-book" />
		WHERE publisher = #{VALUE}	
	</select>
	
	<!-- 
		특정 가격 이상인 책만 검색 
		>= ,<= 가 들어가면 태그가 닫히는 것으로 안다
		&gt; 또는 &gl; 을 사용하자
	-->
	<select id="searchByPrice" parameterType="int" resultType= "book">
		<include refid="select-book" /> 
		<![CDATA[ WHERE price >= #{VALUE}]]> 
    </select>
	
	<select id="searchByIsbn" parameterType="string" resultType= "book">	
		<include refid="select-book" />
		WHERE isbn = #{VALUE}	
	</select>
	
	<delete id="delete" parameterType="string">
		DELETE
		FROM book
		WHERE isbn = #{VALUE}	
	</delete>
	
	<select id="getIsbn" resultType= "string">
		<include refid="select-book" />
		WHERE isbn = #{VALUE}	
	</select>
	
	<!--  특정 isbn 에 해당하는 Book의 title, author, description 정보 수정하기 -->
	<update id="update" parameterType="book" >		
		UPDATE book
		SET title = #{title} , author =#{author} , description= #{description}
		WHERE isbn = #{isbn}
	</update>
	
	
</mapper>

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 